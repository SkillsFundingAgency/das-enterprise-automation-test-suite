trigger: none
pr: none

schedules:
- cron: "0 0 * * *"
  displayName: Daily midnight run
  branches:
    include:
    - DASD-9766-courses-api
  always: true

extends:
  template: ../../pipeline-templates/resource-template.yml
  parameters:
    TestCategory: apprenticecommitmentsapi
    TestDlls: $(Pipeline.Workspace)/das-enterprise-automation-test-suite/drop/tests/SFA.DAS.ApprenticeCommitments.APITests/SFA.DAS.ApprenticeCommitments.APITests.dl
